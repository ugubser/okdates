rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Static assets (logos, etc.) are read-only
      allow read: if request.auth != null;
      allow write: if false;
    }
  }
}
